	moc->version	= 0;
	moc->order		= ...;
	moc->first		= ...;	/* first Healpix index in set */
	moc->last		= ...;	/* 1 + (last Healpix index in set) */
	moc->area		= ...;
	moc->end		= ...;	/* 1 + (offset of last interval) */
